<div class="container">
<%= simple_form_for([ tool, review ], remote: true) do |form| %>
  <%= form.input :description, as: :text %>

  <%= form.input :rating, required: true, :collection => ["1", "2", "3", "4", "5"] %>
  <%= form.button :submit, class: "btn-primary"%>
<% end %>
</div>
